#include "monty.h"
/**
 * sm_stack - this prints the top
 * @head: the head of the stack
 * @counter: code line position
 * Return: nothing
 */
void sm_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	store.flag_change = 0;
}
