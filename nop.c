#include "monty.h"
/**
  *f_nop- does nothing
  *@head: the head of the stack
  *@counter: code line position
  *Return: nothing
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
